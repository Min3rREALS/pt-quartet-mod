if global.panic
prompt_array = [tv_create_prompt(choose("We will see when the little pillar thing that we see everywhere reaches the end of the bar if anything remotely interesting happens."), tvprompt.trigger, spr_tv_idleanim1, 2.5)]
